# 🏢 PGMS – Paying Guest Management System

PGMS is a **Spring Boot + PostgreSQL** based backend application for managing PG operations.  
It handles **Tenant Onboarding, Rooms & Beds allocation, Billing & Receipts, Notices, Dues Registry, Wallet/Referrals, Login/Auth, and Razorpay Integration**.

---

## 🚀 Tech Stack

- **Java 21**
- **Spring Boot 3.3**
- **Spring Data JPA + Hibernate**
- **PostgreSQL 16**
- **Maven**
- **Razorpay SDK (Payment Integration)**
- **JUnit & Mockito (Testing)**
- **H2 (Optional for Dev/Testing)**

---

## 📂 Project Structure

```
src/main/java/com/pgms
│── api/            # Controllers (REST APIs)
│── service/        # Business logic
│── repo/           # JPA Repositories
│── domain/         # Entities
│── dto/            # Data Transfer Objects
│── util/           # Utility classes (logging, enums, constants)
│── config/         # Configurations (DB, Swagger, etc.)
```

---

## ⚙️ Setup Instructions

### 1. Clone Repo
```bash
git clone <your-repo-url>
cd pgms
```

### 2. Install Dependencies

#### Install Java 21
- **Windows (using Chocolatey):**
```powershell
choco install zulu21
```
- **Linux (Ubuntu):**
```bash
sudo apt update
sudo apt install openjdk-21-jdk
```
- **Verify:**
```bash
java -version
```

#### Install Maven
- **Windows:**
```powershell
choco install maven
```
- **Linux:**
```bash
sudo apt install maven
```
- **Verify:**
```bash
mvn -v
```

#### Install PostgreSQL 16
- **Windows:** Download from [https://www.postgresql.org/download/](https://www.postgresql.org/download/)
- **Linux:**
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
```
- **Verify:**
```bash
psql --version
```

---

### 3. Configure PostgreSQL
- Create database `pgms`
```sql
CREATE DATABASE pgms;
```

- Default credentials (from `application.yml`):
  ```yaml
  username: postgres
  password: Chinni@321
  ```

> Change in `src/main/resources/application.yml` if required.

### 4. Run DB Scripts
Inside `docs/db/` you will find:
- `schema.sql` → creates tables with constraints & indexes
- `sample_data.sql` → inserts demo PG, tenants, rooms, etc.

Run them before starting the app.

### 5. Start Application
```bash
mvn spring-boot:run
```
The server starts at:  
👉 http://localhost:8080/api/v1

---

## 🔑 Authentication / Login

- Users can be **Admin / PG Owner / Tenant**.
- Initial login uses a **temporary password** (generated by Admin).
- On first login, users must **reset password**.
- Auth is **username (email/phone) + password** based.
- Optional **TOTP (Google/Microsoft Authenticator)** can be enabled later.
- Reset flow supports **Admin static reset**, **Owner & Tenant reset via Admin**.

---

## 📌 Features

### ✅ Tenant Management
- CRUD operations
- Bed allocation (auto enforce availability)
- Search + Pagination

### ✅ Room & Bed Management
- Multi-bed support
- Bed status: Available / Occupied / Maintenance
- Tenant allocation enforced

### ✅ Billing & Receipts
- Generate monthly bills
- Record receipts & payments
- Auto update dues

### ✅ Notices & Settlements
- Serve notice to tenants
- Track notice period & settlements

### ✅ Dues Registry
- Maintains outstanding dues
- Blocks tenant deletion if dues pending

### ✅ Referrals & Wallet
- Referral credits for tenants
- Wallet linked to tenant account

### ✅ Razorpay Integration
- Order creation
- Payment verification via callback

### ✅ Message Preview
- API to preview reminder messages before sending

---

## 📬 API Documentation

- Full **OpenAPI Spec** available: `docs/openapi.json`
- **Postman Collection** available: `docs/postman_collection.json`

---

## 🧪 Running Tests

```bash
mvn test
```

Covers:
- Unit tests (services, utils)
- Integration tests (REST endpoints with H2 DB)

---

## 📝 Roadmap

- WhatsApp/SMS notification integration
- Advanced reporting dashboards
- Multi-tenant PG (Org level separation)

---

## 👨‍💻 Contributing

1. Fork repo
2. Create feature branch (`feature/xyz`)
3. Raise PR

---
